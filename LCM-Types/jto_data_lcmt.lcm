struct jto_data_lcmt
{
    double cpu_opt_time_microseconds;
    double t_sent;
    double time_start;
    double x_opt[360];
    double u_opt[720];
    double x_warm_start[360];
    double u_warm_start[720];
}
